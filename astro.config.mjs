// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
	site: 'https://senescentgaming.com',
	// Set to '/' once senescentgaming.com DNS points at GitHub Pages —
	// until then the live URL is the project subpath below.
	base: '/senescentgaming-com',
});
